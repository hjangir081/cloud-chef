import 'package:flutter/material.dart';

class CodeChef extends StatelessWidget {
  const CodeChef({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp.router(
      debugShowCheckedModeBanner: false,

    );
  }
}
